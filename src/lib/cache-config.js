/** Seconds before cached public site page data is refreshed. */
export const SITE_PAGE_REVALIDATE = 60;

/** Page routes must use `export const revalidate = 60` (literal; Next.js does not allow imports). */
